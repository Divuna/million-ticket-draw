/**
 * Legacy admin bottom bar — removed in favor of global BottomNavigation + AdminLayout admin nav.
 * Kept as a no-op for any stale imports during migration.
 */
export { AdminContextSubNav } from '@/components/admin/AdminContextSubNav';

export function AdminMenu() {
  return null;
}
