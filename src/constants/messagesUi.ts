/** Display label for rows with sender === "ai" (DB value unchanged). */
export const AI_ASSISTANT_BOB_LABEL = "AI asistent Bob" as const;

/** Typing indicator subline — avoids generic "AI" without Bob. */
export const AI_ASSISTANT_TYPING_SUBLINE = "Píše odpověď…" as const;
