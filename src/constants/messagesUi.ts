/** Display label for rows with sender === "ai" (DB value unchanged). */
export const AI_ASSISTANT_BOB_LABEL = "AI asistent Bob" as const;

/** Typing indicator lead text; animated dots are rendered in Messages.tsx (CSS only). */
export const AI_ASSISTANT_TYPING_SUBLINE = "Bob píše" as const;
